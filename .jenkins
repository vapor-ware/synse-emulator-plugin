#!/usr/bin/env groovy

// Include this shared CI repository to load script helpers and libraries.
library identifier: 'vapor@goreleaser-pipeline/init', retriever: modernSCM([
  $class: 'GitSCMSource',
  remote: 'https://github.com/vapor-ware/ci-shared.git',
  credentialsId: 'vio-bot-gh',
])


// Declared in vapor-ware/ci-shared/vars/golangPipeline.groovy
golangPipeline([
  "image": "vaporio/emulator-plugin",
])
